# Step 1. Read np04 pickles files, select beam events, search for saturated events for each channel --> output json files with results
1:
  name: "Analysis1"
  parameters_file: "params.yml"
  overwriting_parameters: ''